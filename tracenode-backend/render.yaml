services:
  - type: web
    name: tracenode-backend
    env: python
    buildCommand: sed -i -e 's/\r$//' build.sh && chmod +x build.sh && ./build.sh
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
